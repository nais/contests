fasit:
  tenant:
    name: "tenant"
  env:
    name: "env"
contests:
  image:
    repository: europe-north1-docker.pkg.dev/nais-io/nais/images
    name: contests
  accessPolicy:
    enabled: true
  storage:
    enabled: true
  bigquery:
    enabled: false
  kafka:
    enabled: false
    pool:
  opensearch:
    enabled: false
    project:
alerts:
  enabled: true
blackboxExporter:
  additionalTargetURLs:
